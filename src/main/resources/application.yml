#spring:
#  application:
#    name: api-gateway
#  config:
#    import: "configserver:${CONFIG_SERVER_URL:http://localhost:8888}"
#
#  cloud:
#    config:
#      uri: http://config.platform:8888
#
#server:
#  port: 8080
#
#management:
#  endpoints:
#    web:
#      exposure:
#        include: health,info


spring:
  application:
    name: api-gateway
  config:
    import: "configserver:"
  cloud:
    config:
      uri: http://config.platform:8888

server:
  port: 8080